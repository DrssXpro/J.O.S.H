// 图表初始配置(px)
export const chartInitConfig = {
	x: 50,
	y: 50,
	w: 500,
	h: 300,
	offsetX: 0,
	offsetY: 0
};

// 工作区域历史记录存储最大数量
export const HistoryMax = 30;

// 工作台大屏背景图片大小限制（5M）
export const backgroundImageSize = 5;
